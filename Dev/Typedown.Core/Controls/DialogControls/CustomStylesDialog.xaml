<controls:AppContentDialog
    x:Class="Typedown.Core.Controls.DialogControls.CustomStylesDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Typedown.Core.Controls.DialogControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Typedown.Core.Controls"
    xmlns:models="using:Typedown.Core.Models.RuntimeModels"
    xmlns:u="using:Typedown.Core.Utilities"
    Style="{StaticResource DefaultAppContentDialogStyle}"
    Title="{u:LocaleString Key=CustomStylesDialog.Title}"
    CloseButtonText="{u:LocaleString Key=Close}"
    Unloaded="OnUnloaded"
    mc:Ignorable="d">
    <StackPanel Spacing="12">
        <TextBlock Text="{u:LocaleString Key=CustomStylesDialog.Description}" TextWrapping="WrapWholeWords"/>
        <Button HorizontalAlignment="Left" MinWidth="120" Style="{ThemeResource AccentButtonStyle}" Click="OnAddButtonClick">
            <TextBlock Text="{u:LocaleString Key=Add}"/>
        </Button>
        <ListView ItemsSource="{x:Bind CustomStyles, Mode=OneWay}" SelectionMode="None">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem" BasedOn="{StaticResource DefaultListViewItemStyle}">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0,0,0,4"/>
                    <Setter Property="IsTabStop" Value="False"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:CustomDocumentStyle">
                    <controls:NormalSettingItem Title="{x:Bind Name, Mode=OneWay}">
                        <Button Tag="{x:Bind}" Click="OnDeleteButtonClick" MinWidth="100">
                            <TextBlock Text="{u:LocaleString Key=Delete}"/>
                        </Button>
                    </controls:NormalSettingItem>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</controls:AppContentDialog>
